@model IEnumerable<WebDT.Models.EF.Category>

<div class="brands-area">
    <div class="zigzag-bottom"></div>
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <div class="brand-wrapper">
                    <div class="brand-list">
                        @foreach (var i in Model)
                        {
                               <img src="@i.link" alt="">
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>
</div> <!-- Kết thúc thương hiệu khu vực -->


